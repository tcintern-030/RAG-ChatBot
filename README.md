# RAG-ChatBot
A web-based RAG chatbot that allows users to upload documents and ask questions about their content. The system processes documents using chunking and embeddings, stores them in ChromaDB, retrieves relevant information based on user queries, and uses an LLM to generate grounded responses using the retrieved context.
